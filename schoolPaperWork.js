function paperwork(n, m) {
  if( n < 0 || m < 0 ){// by default whether n or m is less than zero it still equals 0
    return 0
  }
  else {
    return n * m // if (n) pages  is one schoolwork assignment and i have m number of classmates multiply n number of pagesto see how many pages i need in total to get product (product is when you multiply)
  }
}